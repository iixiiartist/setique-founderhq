# Environment Configuration for Setique Founder Dashboard

# Note: Groq AI API key is server-side only
# Set GROQ_API_KEY in Supabase secrets: npx supabase secrets set GROQ_API_KEY=your_key

# Supabase Configuration
VITE_SUPABASE_URL=your_supabase_project_url_here
VITE_SUPABASE_ANON_KEY=your_supabase_anon_key_here

# Stripe Configuration (Publishable Key - safe to expose in frontend)
VITE_STRIPE_PUBLISHABLE_KEY=pk_test_your_stripe_publishable_key_here

# Stripe Price IDs (from Stripe Dashboard > Products)
# Only active plans - deprecated plans removed
VITE_STRIPE_PRICE_POWER_INDIVIDUAL=price_xxx_power_individual
VITE_STRIPE_PRICE_TEAM_PRO_BASE=price_xxx_team_pro_base
VITE_STRIPE_PRICE_TEAM_PRO_SEAT=price_xxx_team_pro_seat

# Application Configuration
VITE_APP_NAME=Setique Founder Dashboard
VITE_APP_VERSION=1.0.0
VITE_ENVIRONMENT=development
VITE_APP_URL=http://localhost:3001

# Optional: Analytics & Monitoring
VITE_ANALYTICS_ID=your_analytics_id_here

# Sentry Error Tracking (get from https://sentry.io)
VITE_SENTRY_DSN=https://your_sentry_dsn@sentry.io/your_project_id

# Sentry Configuration for Source Map Uploads (production builds only)
# These should be set as environment variables in Netlify, not committed to git
# SENTRY_ORG=your-org-slug
# SENTRY_PROJECT=your-project-slug
# SENTRY_AUTH_TOKEN=your-auth-token

# Note: Stripe Secret Key should ONLY be used in Supabase Edge Functions, never in frontend
# Set STRIPE_SECRET_KEY in Supabase Dashboard > Project Settings > Edge Functions > Secrets