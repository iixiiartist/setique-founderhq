# Netlify redirects

# NOTE: Edge function og-meta.ts handles /share/brief/*, /share/report/*, /forms/*
# for social crawlers. Regular browsers fall through to the SPA.

# SPA fallback - all other routes go to index.html
/*    /index.html   200
